// import fs from "fs"

export const program = "34 35 + write 500 80 - write"
/* I will disable this first.
const args = process.argv.slice(2)
console.log(args)

export const cutNewline = function(charlist) {
   if (charlist === undefined) {
      charlist = '\s'
   }
   return this.replace(new RegExp("^[" + charlist + "]+"), "")
}

export const program = fs.readFileSync(args[0], "utf-8").replace('\n', '\\n').replace("\\n", " ").trimEnd()
if (program == undefined) {
   throw Error, `Oops, we cannot read ${args[0]}`
}
   */
// TODO: Write a file handling tool -- DONE
